{
  "name": "payday-qa-workshop",
  "version": "1.0.0",
  "description": "Payday salary-run demo app — target system for the AI in Testing workshop",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "^1.62.0",
    "vite": "^8.0.16"
  }
}
