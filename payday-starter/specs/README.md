# Test plans

Plans written by the test-planner agent land here, one file per feature:
`<feature-slug>.plan.md`.

A plan is reviewed by a human before any test code is generated from it. Deleting a
low-value scenario at this stage costs nothing; deleting the test it produced costs a
review cycle.
