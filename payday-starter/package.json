{
  "name": "payday-starter",
  "version": "1.0.0",
  "description": "Payday salary-run app — no tests, no test runner, no CI. That is the point.",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^8.0.16"
  }
}
